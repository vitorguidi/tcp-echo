# C++20 everywhere — needed for coroutines in stages 9–11
build --cxxopt=-std=c++20
build --host_cxxopt=-std=c++20

# Treat warnings as useful noise, not errors, while learning
build --cxxopt=-Wall
build --cxxopt=-Wextra
