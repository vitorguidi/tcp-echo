# C++23 everywhere — needed for std::generator and coroutines in stages 7–11
build --cxxopt=-std=c++23
build --host_cxxopt=-std=c++23

# Treat warnings as useful noise, not errors, while learning
build --cxxopt=-Wall
build --cxxopt=-Wextra
